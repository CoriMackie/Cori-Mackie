# Prompt Log

A running record of AI sessions that mattered — not every exchange, only
the ones that shaped something in this repository.

| Date       | Session                          | Outcome                                                            |
| ---------- | --------------------------------- | ------------------------------------------------------------------- |
| 2026-08-23 | GitHub account onboarding scaffold | Created the standard repo layout (`AGENTS.md`, `CLAUDE.md`, `prompt-log.md`, `.gitignore`, `capabilities/`, `docs/briefs/`, `docs/decisions/`, `data/`, `analysis/figures/`, `.claude/skills/`) and renamed `resume` to `RESUME.md`. |
| 2026-08-29 | Stage 1 brief cleanup and critique after grading feedback | Moved the brief to the graded path `docs/briefs/perfect-competition-brief.md`, reformatted it, and deleted the unused `initial-case-brief.md` template. Asked for the marginal-cost arithmetic behind each crop: it showed tomato bed 11 costs about $9,390 against a price of $8,800, that carrots and mesclun stay profitable past their caps, and that 14 tomato beds would exceed the farm's 6,480-hour labor ceiling. That changed my carrot call from 0 beds to 20 and my mesclun call from 43 to the 30-bed cap. The reasoning and the prose in the brief are mine, rewritten after AI critique; AI supplied the arithmetic, the per-bed cost table, and the review of my drafts. |
| 2026-09-09 | Stage 1.2 rebuild of `model.xlsx` from the corrected spec | Rebuilt `capabilities/marginal-analysis/model.xlsx` from the current `spec.md`: four sheets (Inputs, Marginal Analysis, Optimization, Validation), 57 named ranges, 791 formulas, and one typed constant outside the Inputs sheet (the hand-computed 99 hours V1 compares against). Fixed the four defects the first build carried: the Plant/Stop column now implements V7's first-crossing rule and no longer restarts, the validation area exists and computes every check as OK/VIOLATED, `BLENDED_RATE` drives the per-crop P&L, and the three bed counts are named decision variables. Check figures reproduce: 10/20/30 beds, $42,761.66 season profit, crossings at 10/10/6, tomato MC dip at bed 6 of $2,754.58. V2 and V3 are built as check cells but read VIOLATED until the Farm Profit Lab figure and the two Solver runs are entered — they cannot be computed inside the workbook. AI built the workbook and listed the spec's remaining ambiguities and undefined terms; the specification and the audit write-up are mine. |
| 2026-09-09 | Stage 2 Solver debugging and model validation | Solver reported "has not executed this optimization". Cause: the workbook held three per-sheet Solver models — the real one on `Optimization`, plus two junk ones on `Inputs` and `Validation` with no objective and an invalid engine, one of them pointing its changing-cells at a prose cell. Stripped both; the working model was left untouched. Validated the workbook by recalculating it headlessly and sweeping all 9,726 feasible bed combinations through the live sheet: 10/20/30 at $42,761.66 is the unique global optimum, and every feasible start climbs to it, so there are no local optima. Found V3 could never pass as written — its second start point, 20/0/0, is infeasible and stranded, so Solver returns no profit figure for the check to read — and changed it to 15/0/0. AI did the diagnosis, the sweep and the drafting of `spec.md`/`README.md`; the model, its conventions and the result are mine. |
| 2026-09-10 | Repository and account tidy-up | Audited the folders against the structure in `AGENTS.md`. Every directory was in use or deliberate scaffolding except `analysis/README.MD` — an empty 1-byte file with a mismatched uppercase extension, created through the GitHub web UI to hold the folder before `analysis/figures/README.md` existed. Deleted it, and dropped the Repository Guide's links to `CM_CV.md` and `Cori_Mackie_Bio.md`, neither of which is in the repo. Also flagged two disposable repositories in the account: `Case---2` (public, zero commits) and `desktop-tutorial` (the GitHub Desktop onboarding repo). |
| 2026-09-12 | Stage 1.2 feedback — the memo's verification claim | The decision memo said every validation check read OK. The workbook as committed displayed **VIOLATED** on V2 and V3: the observed values were sitting in their cells, but the two check cells still held the result from before those values were entered, because the last write to the file was made by a tool that does not recalculate formulas. Refreshed the two stale results, set the workbook to recalculate when it opens so a check cell cannot show a stale answer again, and rewrote the memo's "How far this was checked" section — it now separates the checks the workbook computes for itself from V2 and V3, which rest on figures I entered from outside the sheet, and it keeps the correction on the page rather than quietly editing the claim away. AI found the stale cells, made the workbook fix and drafted the replacement section; the decision to leave the correction visible is mine. |
