# Capabilities

One folder per capability. Each capability folder is self-contained and
holds:

-   `README.md` — what this capability does and how to use it
-   `spec.md` — the approach/spec behind it
-   a supporting model or workbook (e.g. `model.xlsx`), where relevant

No capabilities have been added yet.
